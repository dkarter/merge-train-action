version: '3'

includes:
  ci: ./taskfiles/ci.yml

tasks:
  setup:
    desc: Install Bun dependencies
    cmds:
      - bun install

  package:
    desc: Bundle TypeScript action into dist/
    cmds:
      - bun run package
